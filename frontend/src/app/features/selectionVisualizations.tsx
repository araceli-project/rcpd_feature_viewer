import { FeatureData } from "../d3_visualizations/feature_data";

export default function GenerateSelectionVisualization({
  selectedPointIndices,
  featureData,
}: { selectedPointIndices: number[]; featureData: FeatureData }) {
    return (
    <div>Selection Visualization</div>
    )
}